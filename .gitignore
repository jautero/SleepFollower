# Final packages
RPMS/*

# Binaries are created in the project folder (Sailfish SDK doesn't support shadow building yet)
harbour-sleepfollower-pro-sailfish
src/harbour-sleepfollower-pro-sailfish
tests/tst-harbour-sleepfollower-pro-sailfish

# Something Qt Creator generates
debugsources.list
documentation.list
debugfiles.list
debuglinks.list

# object files, makefiles, environment-specific Creator settings
*.o
Makefile
*.pro.user
*.pro.user.*